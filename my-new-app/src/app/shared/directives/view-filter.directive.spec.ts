import { ViewFilterDirective } from './view-filter.directive';

describe('ViewFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewFilterDirective();
    expect(directive).toBeTruthy();
  });
});
