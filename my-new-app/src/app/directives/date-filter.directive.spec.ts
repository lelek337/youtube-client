import { DateFilterDirective } from './date-filter.directive';

describe('DateFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new DateFilterDirective();
    expect(directive).toBeTruthy();
  });
});
