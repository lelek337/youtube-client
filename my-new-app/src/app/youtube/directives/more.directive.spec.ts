import { MoreDirective } from './more.directive';

describe('MoreDirective', () => {
  it('should create an instance', () => {
    const directive = new MoreDirective();
    expect(directive).toBeTruthy();
  });
});
